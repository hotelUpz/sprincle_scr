1. Gfhcth Coin Geko полностью удаляем и заменяем на реши=ение которое реализовано в https://github.com/hotelUpz/arb9_alpha/tree/v6

2. Добавляем еще 2 биржи: Kucoin и Bitget. В настройках биржу Phemex ставим как субдоменанту то есть ведомую.

3. Во всех настройках ставим "across_funding" == "3" -- уже сделал.

4. "source_of_trus": "bid1_ask1", -- либо удаляем полностью (так как по сути у нас только такая истина и есть, либо подчиняем к=новому концепту).

Новый концепт: (фандинги не удаляем но просто везде отключаем -- в данной концепции сигналов они не принимают участия (возможно когда то понадобсятся поэтому не удаляем а просто отключаем -- в конфигах.

Второе: вводим два типа сигналов, Сигнал Типа1 и Сигнал типа2. Сигнал типа один в качестве сырья смотри та спреды между bid1 одной биржи (например пхемекс) и ask1 биржи доминанты (например бинанса или других) -- и проверяет условие спреда (с учетом проверки какую сторону проверяем Лонги или Шорты). Для этого я сейчас заведу отдельные настройки. сигнал типа 2 делает все то же только в качестве сырья проверяет спред между bid1 биржи ведомой и bid1 биржи ведущей. То же проделываем и с асками -- пилим сюда: evaluate в # File: CORE/signal_evaluator.py

и каждый тип сигнала перед отправкой помечаем как Типа1 или Типа2. Информацию о фандинге оставь подсветкой и пометь как отдельный блок.

Новая концепция настроек представлена в файле настроек CONFIG/new_rules.json, старая -- CONFIG/rules1.json